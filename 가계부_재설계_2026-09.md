# 가계부(ledger) 재설계 — 2026-09

> **이 문서의 용도**: 다음 채팅 세션(들)이 **이 문서만 읽고** 코드를 작성·수정할 수 있게 쓴 설계 명세다.
> 설계 세션(2026-09-05)에서는 소스 코드를 한 줄도 바꾸지 않았다. 구현은 §7의 단계(A~F)를 **한 세션에 한 단계**씩
> 진행한다. 각 단계 끝에 "완료 기준"과 "CLAUDE.md 갱신 항목"이 있으므로 그대로 따르면 된다.
>
> **다음 세션 시작 프롬프트(복사용)** — §11 참조. **구현 전 사용자 확인이 필요한 결정 6건** — §9.

---

## 0. 한 줄 요약

지금 가계부는 **"항목 × 월 매트릭스(계획/실제)" + 대출 계산기**로, 스프레드시트를 그대로 옮긴 형태다.
이 강점(월 예산표·대출·연단위·검증 규약)은 **그대로 두고**, 그 아래에 **거래 원장(날짜별 지출 입력)**을
깔아 실제 금액이 거래에서 자동으로 올라오게 하고(월 매트릭스 = 예산 레이어, 거래 = 기록 레이어),
그 위에 **예산 진행률·고정지출 확인·카드 결제일/할부·이체·대출 상환표·월 마감·순자산**을 얹는다.
참고 앱들이 공통으로 가진 것 중 이 앱에 없던 것만 골랐고, 자동 연동(문자 파싱·은행 API)·사진 첨부·
다중 통화·공동 편집·급여일 기준 월 경계는 **범위 밖/보류**로 못 박았다(§6·§9).

---

## 1. 현재 구현 (2026-09-05, 커밋 `9e12688`)

### 1.1 파일과 역할

| 파일 | 줄 | 역할 |
|---|---|---|
| `src/ledger.ts` | 1,713 | 타입 + 순수 로직(⚠️ `@ts-nocheck` 금지, 상대 import에 `.ts` 확장자 필수, React/DOM 금지, `enum` 금지). `scripts/verify-ledger.mjs`가 **직접 import** |
| `src/components/LedgerPage.tsx` | 2,153 | 화면 본체. overlay(인앱 폴백)/page(별도 창) 겸용. 로컬 사본 + 2.5초 idle 승격 |
| `src/components/LedgerWindow.tsx` | 162 | 별도 창(`/?ledgerWindow=1`) 셸. `ledger:live` 수신 / `ledger:books`·`ledger:snapshots`·`ledger:ping` 송신. 수신 화이트리스트는 **열거형** |
| `src/ledgerExcel.ts` | 686 | 엑셀 3시트(① 월 매트릭스 29열 · ② 대출 12열 · ③ 연간요약 7열). `buildLedgerSheets`/`downloadLedgerXlsx` |
| `src/components/CalendarModal.tsx` | (부분) | 메모 달력 BUDGET 칩 — `ledgerEventsByDate` 라이브 파생(`:408~437`), 패드(`:1158~1210`), 칩 라벨 `:670~673` |
| `src/App.tsx` | (부분) | 영속화·게이팅·브릿지·flush/exit 커밋(1.4) |
| `src/hooks/useDriveSync.ts` | `:50~62` | `_preserveStickyPersonalData` — 백업 복원 sticky |
| `scripts/verify-ledger.mjs` | 1,747 | 검증 **489건**(파트① 직접 import #0~#104e / 파트② 소스 가드 #G1~#G38e). `stripComments`·`sliceBlock`·`S` 래퍼 |
| `scripts/validate_palette.mjs` | — | 팔레트 CVD/대비 검증(`ledgerRamp` 사본 1:1 대조 §0, 결제수단 독립 hue 불가 §4) |

### 1.2 데이터 모델 (저장 = Drive STATE `ledgerBooks` · 앱 레벨 `ledgerSnapshots`)

```
LedgerBook { id, name, items: LedgerItem[], categories: string[], months: { [ym]: { touchedDate, memo } }, createdAt, updatedAt }
LedgerItem { id, group: 'loan'|'fixed'|'variable'|'annual'|'income', pay: 'cash'|'card'|'transfer'|'auto'|'other',
             name, category(구분·자유문자열), plan: number|null, planUnit: 'month'|'year',
             planOverride: {[ym]: number}, actual: {[ym]: number},   // ⚠️ 키 유무 = 미입력/0원 구분
             memo, activeFrom, activeTo (ym), dueMonth, dueDay (annual 전용), loan: LedgerLoan|null,
             tone: 'none'|'warn'|'good'|'info', createdAt }
LedgerLoan { principal, principalAsOfYm, annualRate, method: 'interestOnly'|'amortizing'|'equalPrincipal',
             endDate, termMonths, graceMonths, paymentOverride }
LedgerSnapshot { id, savedAt, label, auto, books }   // 앱 레벨, 10개 / 512KB 상한
```
- 상한: 장부 5 / 항목 200 / 월 240 / 구분 40 (`ledger.ts:191~199`).
- 파생만 있고 저장하지 않는 것: 월 납입액·합계·증감·예상(expected)·KPI 전부.

### 1.3 화면 (LedgerPage)

- 헤더: 장부 선택·이름, 연도 ◀▶, 기준월 select, ⭳ 엑셀, 💾 저장(스냅샷), 이전 기록, ⧉ 새 창, ✕.
- KPI 6장(`:1327~1362`): 월 지출 합계(연단위 제외) / 예상 月 지출(+결제수단 칩·계획 기준) / 수입(월) / 저축여력 / 대출 월 납입 / DSR.
- 탭 4개(`:1387` `['matrix','loan','chart','annual']`): **월 매트릭스**(항목 × 12월 계획·실제·차이, 소계·결제수단 소계·총계, 월 숨김,
  ▲▼ 순서, 구분 select, 적용기간 잠금 칸 클릭, 계획 자동 반영 흐린 이탤릭) / **대출** / **분석**(계획 대비 실제·전월 대비·구분 도넛·
  상세 도넛·결제수단 막대·수지 균형) / **연간**(연도별 전년 대비·연단위 일정).
- 확인창은 전부 인라인 2단계(별도 창에는 App이 없어 ConfirmDialog·토스트가 안 뜬다). `notify()` 호출 0곳.

### 1.4 영속화·브릿지 배선 (App.tsx)

| 지점 | 위치 |
|---|---|
| state | `:429~433` `ledgerBooks`·`ledgerSnapshots` / `:254` `ledgerDataState` |
| 게이팅 | `:440` `ledgerAccess = 관리자 || userFeatures.ledgerEnabled`(approved_users M열) |
| 로드 | `:953~954` `applyStateData` / `:1021~1026` `applyBackupData`(sticky = `ledgerBooksHaveContent`) |
| 지문 | `:3626~3627` `ledgerFingerprint`·`ledgerSnapshotsFingerprint` → `portfolioStructureKey` |
| 저장 리터럴·deps | `:3651`, `:3666` / 계좌 0개 예외 `:3515` |
| flush/exit | `:2122~2142` `ledgerFlushRef`·`ledgerExitCommitRef` → `exitCommitRef` 4원 합성 `:2155` / 수동 저장 4핸들러 `:2950·3162·3184·3205` |
| 브릿지 | `:2832~2855` `ledger:live`(books·snapshots·hideAmounts·today·readOnly·dataState) / `:2861~2884` 수신 `ledger:ping`·`ledger:books`·`ledger:snapshots`(impersonation fail-closed) |
| 창 열기 | `:2921~2936` `openLedgerWindow`(팝업 차단 시 인앱 폴백 `showLedgerPage`) |
| 인앱 마운트 | `:4955~4978` `readOnly = adminViewingAs || ledgerDataState !== 'ready'` |
| 달력 | `:2245` `calendar:live`에 `ledgerBooks` 동봉 / `:4895~4896` CalendarModal props / `:2292~2299` `calendar:openLedger` |
| 진입 | `UserInfoBar.tsx:320~328` 아이콘 / `main.tsx:25·34` `LEDGER_WINDOW_BOOT` |

**핵심 불변식(재설계에서도 유지)** — writer는 앱 탭 하나 · 창은 App 미마운트 · `window.open`에 `noopener` 금지 · `ping.need`가 초기 전송의
유일한 트리거 · 끊기면 `readOnly` · Drive 로드 완료(`dataState==='ready'`) 전 편집 잠금(fail-closed) · 정규화 멱등 · 지문은 절대 throw 금지 ·
sticky 판정은 `length`가 아니라 내용 · 미입력≠0원 · 색은 손익색(빨강/파랑) 금지, 상태색(amber ▲ / teal ▼)+아이콘·라벨 동반 · 새 hue 금지.

---

## 2. 진단 — 살릴 것과 고칠 것

### 2.1 장점 (그대로 유지 — 재설계가 깨뜨리면 안 되는 것)

| # | 장점 | 근거 |
|---|---|---|
| S1 | **월 예산표 모델** — 항목 × 12월, 계획/실제/차이, 계획 자동 반영(expected), 소급 방지(`planOverride`), 적용기간(`activeFrom/To`) | 스프레드시트 사용자의 실제 습관. Actual Budget의 "category × month" 그리드·크몽 구글시트 템플릿의 Plan/Monthly Report와 같은 골격 |
| S2 | **미입력 ≠ 0원** null 계약 + 비교 불가 시 `'-'`(거짓 −87% 방지) | `ledger.ts:658·1093`. YNAB/Actual도 '미분류·미승인'을 값이 아닌 상태로 다룬다 |
| S3 | **대출 엔진** — 3방식·거치·직접입력 우선·잔액 기준월 앵커·향후 12개월 합·DSR | 편한가계부·머니매니저에는 상환표 자체가 없다. 엑셀 사용자는 PMT/PPMT 상환대장을 따로 둔다 |
| S4 | **연단위 목돈**(dueMonth/Day) → 달력 칩 + 예상 月 지출에 ÷12 반영 | YNAB Rule 2(true expenses)·Actual `by` 템플릿을 이미 구현한 셈 |
| S5 | 영속화 안전망 — sticky·이전 기록·로드 전 잠금·idle 승격·별도 창·정규화 멱등 | 2026-08-29 유실 사고 이후 다진 규약 |
| S6 | 엑셀 3시트 · 분석 차트 6종 · CVD 검증 팔레트 · 489건 검증 | 국내 사용자의 앱 선택 3대 조건 중 하나가 **엑셀 백업**이다 |
| S7 | 결제수단 축 소계·100% 스트립 | 위플 최다 요청("현금·카드 따로 보기")을 이미 충족 |

### 2.2 단점·공백 (참고 앱과 실제 사용자 습관 대비)

| # | 공백 | 지금 상태 | 증상 |
|---|---|---|---|
| W1 | **거래(일별) 입력이 없다** | 항목 × 월 합계만 저장 | 변동비를 월말에 영수증을 합산해 한 칸에 적어야 한다. 조사한 예산 앱 전부가 거래 원장이 저장 단위이고 매트릭스는 뷰다 |
| W2 | 달력 연동이 얇다 | BUDGET 칩 = '정리한 날' + 연단위 예정일 | "그날 얼마 썼나"를 달력에서 볼 수 없다(토스·뱅샐·편한·위플 전부 달력 뷰) |
| W3 | 예산 **진행률**이 없다 | 월말 차이(▲▼)만 | "예산 몇 % 썼나·오늘 얼마 쓸 수 있나"(뱅샐의 일일 가능액)가 없다 |
| W4 | 고정지출 **확인 워크플로**가 없다 | 계획이 자동 반영될 뿐 | 월세·보험이 실제로 나갔는지 체크할 곳이 없다(엑셀 사용자의 '고정지출 점검표') |
| W5 | **카드 결제일·마감일·할부**가 없다 | `pay:'card'` 라벨뿐 | 국내 최장수 요구 — 클머니 사용자를 10년 붙잡은 기능. "이번 달 청구 예정"·할부 잔여 표현 불가 |
| W6 | 계좌·잔액·**이체** 개념이 없다 | 결제수단은 라벨 | 카드대금 이체·적금 이체가 지출로 이중 계상되는 자동연동 앱의 고질병을 **구조로** 막을 장치가 없다 |
| W7 | 대출은 **월 납입액**까지만 | 상환표·조기상환·금리변경 없음. ⚠️ 원리금균등의 `interestPart`가 매달 `P×i`로 고정(`ledger.ts:553`) — 납입액은 맞지만 원금/이자 분해는 첫 회차만 정확 | 잔여 원금·총이자·조기상환 효과를 볼 수 없다. 상환표를 만들면 지금 분해로는 틀린다 |
| W8 | 자산(포트폴리오)과 **연결이 없다** | 앱이 전 계좌 평가액·입출금 원장을 이미 갖고 있음 | 순자산(자산−부채)을 가계부에서 볼 수 없다(Mint 종료 이후 최다 요구) |
| W9 | 구분(category)이 1단 | `group`(성격) + `category`(자유문자열) | "식비 > 외식/장보기" 2단 분석이 안 된다(Firefly의 "No sub categories"가 대표 불만) |
| W10 | 검색·필터·행 메모·**분할**·**누가 썼는지** 없음 | 항목 메모만 | 과거 지출을 찾을 수 없고, 한 거래를 두 항목으로 나눌 수 없다(뱅샐·머니매니저의 결함으로 지적됨) |
| W11 | **입력 마찰**이 크다 | 데스크톱 매트릭스 셀 편집 | "3탭 입력"(머니매니저)·최근 항목 제안·모바일 폭 대응이 없다. 가계부 이탈의 1원인은 daily-logging tax |
| W12 | 월 **마감(결산)** 개념 없음 | `touchedDate`만 | 장기 사용자의 공통 습관(잔액 대조→누락 보충→다음 달 예산)을 담을 곳이 없다 |
| W13 | 이월(rollover)·저축 목표 없음 | — | 봉투식 예산·목표 적립 진행률 불가 |
| W14 | 가져오기 없음(내보내기만) | — | 기존 스프레드시트·타 앱 사용자의 이관 비용이 크다 |
| W15 | 다가오는 지출이 없다 | 달력 칩만 | 14일 내 납부 예정(연단위·카드 결제일·고정지출·대출)을 한 곳에서 볼 수 없다 |
| W16 | 연간 분석이 얇다 | 연도별 막대 + 전년 동월 | 구분별 12개월 추이·구분별 전년 대비가 없다 |

---

## 3. 참고 조사 요약 (2026-09-05, 웹 조사 3건 통합 — 출처는 각 항목)

### 3.1 오픈소스 앱 공통 패턴 (17종 조사: Actual ★28.6k·Firefly III ★24.5k·Maybe/Sure·HomeBank·GnuCash·MMEX·Ivy·Cashew·Buckwheat·Beancount/Fava·hledger·Financier·Kresus·Ghostfolio·Bluecoins·Money Manager)

| 패턴 | 근거 앱 | 이 설계의 반영 |
|---|---|---|
| **저장 단위는 거래, 항목×월은 뷰** — 분할·이체·환급·규칙·일정·대조가 전부 거래 위에서만 성립 | YNAB API(`subtransactions`·`transfer_account_id`), Actual, Firefly, GnuCash | §5.1 두 레이어. 매트릭스 셀 = 거래 합(거래가 있으면) |
| **분할(split) 거래는 기본기** | Actual·GnuCash·HomeBank·MMEX(분할별 태그)·hledger. 없는 머니매니저는 리뷰에서 결함으로 지적 | `LedgerTx.splits` (단계 A) |
| **이체는 제3의 거래 유형** + 계좌 유형(현금/카드/은행/부채) + **카드 명세 주기(cutoff/due/한도)** | Firefly 출금/입금/이체, Bluecoins "separate transaction type"+카드 cutoff/due, 머니매니저 카드 관리, Cashew 이체 쌍 | `kind:'transfer'`·`LedgerAccount`·카드 청구주기 (단계 C) |
| 정기 거래 = 예약 + **자동 기입 vs 승인** + 예정/미납 표시 + `~` 예상 금액 | Actual schedules(승인/자동, `~`, ±7.5% 근사 매칭), GnuCash 연기 어시스턴트, Bluecoins 주말 처리, Cashew 미지급 제외 | 고정지출 ✔ 확인 + `autoPost` 옵션 (단계 B) |
| 예산 이월 정책은 **사용자가 고른다** | Actual(초과 다음 달 차감/보류), Quicken(없음/전부/양수만), YNAB(옮겨 덮기), Bluecoins carry-over, Firefly auto-budget | `carryover` 옵트인 + 양수만 기본 (단계 B) |
| 카테고리 **계층(대/소)** + 태그는 직교 축 | MMEX 무제한 다단계, Cashew·머니매니저 서브카테고리, Actual 그룹. Firefly "No sub categories" 불만 | `category`에 `'/'` 규약으로 2단 트리 (단계 F) — 스키마 무변경 |
| 규칙 기반 자동 분류(임포트 시점, 우선순위·정규식) | Actual pre/default/post 3단계, Firefly 규칙, HomeBank 정규식, hledger CSV 규칙 | 붙여넣기 가져오기의 **키워드→항목 매칭 규칙**(재분류에서 자동 생성) (단계 E) — 자동연동이 없어 그 이상은 불필요 |
| 목표(싱킹펀드)·저금통·대출/빌린 돈 | Actual `by` 템플릿, Firefly 저금통·부채, Cashew 목표·lent/borrowed, YNAB Loan Planner | 연단위(기존)·`LedgerGoal`·상환표/조기상환 (단계 D·E) |
| 리포트 3종(**순자산·현금흐름·캘린더**) + 예산 대비 | Actual 순자산·현금흐름·캘린더 카드, Bluecoins, Maybe 순자산 추세, HomeBank "Where your money goes" | 순자산(단계 D)·달력 일별 합(단계 A)·구분 추이(단계 F) |
| 임포트는 컬럼 매핑·중복 매칭·**행별 오류 리포트**, 내보내기는 CSV/Excel | Bluecoins 행별 오류, Cashew 컬럼 매핑, GnuCash 중복 매칭. Firefly CSV 엄격함이 불만 | 붙여넣기 가져오기의 부분 성공·사유 행 (단계 E) |
| 로컬 우선·오프라인·**입력 마찰 최소화**(3탭 입력·위젯·알림) | 머니매니저 "I tap a category, type the amount, and it's done", Actual "budget in 15 minutes". 불만은 동기화 마찰·daily-logging tax | 빠른 입력 바 (단계 A). 위젯·푸시는 범위 밖 |
| 성능 | Actual "큰 예산 파일은 편집 불가"(#5619) 상위 이슈, 뱅샐 "느리고 답답" | `txIndexOf` WeakMap 캐시·지문 참조 캐시 (§5.6) |
| 다중 통화 | Actual 부재가 최다 이슈(#2147), 나머지는 계좌별 통화 | **범위 밖** — 해외 자산은 앱의 포트폴리오가 담당 |

주요 출처: [Actual 문서](https://actualbudget.org/docs/budgeting/) · [Actual schedules](https://actualbudget.org/docs/schedules/) · [Actual rules](https://actualbudget.org/docs/budgeting/rules/) · [Actual goal templates](https://actualbudget.org/docs/experimental/goal-templates/) · [Actual 추천순 이슈](https://github.com/actualbudget/actual/issues?q=is%3Aissue%20is%3Aopen%20sort%3Areactions-%2B1-desc) · [Firefly III](https://github.com/firefly-iii/firefly-iii) · [GnuCash features](https://www.gnucash.org/features.phtml) · [MMEX](https://github.com/moneymanagerex/moneymanagerex) · [Cashew FAQ](https://cashewapp.web.app/faq.html) · [Bluecoins 가이드](https://www.bluecoinsapp.com/guide/) · [머니매니저 App Store](https://apps.apple.com/us/app/money-manager-expense-budget/id560481810) · [YNAB API](https://api.ynab.com/) · [Firefly vs Actual](https://selfhostable.dev/blog/firefly-iii-vs-actual-budget-self-hosted-finance/)

### 3.2 국내 앱·실제 사용자 습관

**앱**: 편한가계부(=머니매니저, 수기 앱 사실상 표준: SMS 자동입력·복식부기·자동이체·주/월/연 예산·달력·엑셀·위젯 3종·영수증 사진), 뱅크샐러드(마이데이터 자동연동·**남은 예산÷남은 일수 일일 가능액**·저축 목표·엑셀 메일), 토스(고정지출 자동 분류·예산 없음), 위플(생활비/고정/특별 3분류·**밤 9시 기록 알림**·"현금·카드 분리" 최다 요청·"이월 잔액 삭제 불가" 불만), 똑똑가계부(**월 시작일=카드 결제일**·할부 분할·취소 문자 인식), 클머니(**결제일·휴일 익일 처리·청구할인·이월청구·할부** — 대체 불가였으나 iOS17에서 사망), 후잉(복식부기·10년 사용자·입력 복잡), 카카오페이(이체 이중 계산 불만), 네이버 가계부·브로콜리(서비스 종료 → "불안해서 못 쓰겠다"는 학습된 불신).

**한국 사용자 TOP 10 요구**(조사 결론): ① 고정/변동 2축 + 계획이 손대지 않아도 매달 채워짐(✅ 이미 있음) ② 항목별 + 총액 예산과 셀 색 진행 ③ 결제수단 구분 + **이체는 지출이 아님** ④ **카드 결제일·할부·결제예정금액** ⑤ 대출 상환표(원금=부채 감소, 이자=지출)·자산/부채 현황 ⑥ 달력 뷰 + 항목×월 + 연간 요약(✅ 부분) ⑦ **월 마감 절차** 내장 ⑧ 엑셀 내보내기·PC 큰 화면(✅) ⑨ 부부 공유 — 각자 계정 + **누가 썼는지 구분** ⑩ 저축 목표(수입 대비 %).

**하지 말아야 할 것 5**: ① 이체·카드대금을 지출로 이중 계상 ② 미입력을 0으로 단언, 이월 잔액을 지울 수 없게 ③ 자동연동 전제·수기/백업 소홀 ④ 카테고리·결제수단 커스텀 불가, 거래 분할 불가 ⑤ 내보내기 없는 유료화·업데이트 방치.

**수기 장기 사용자 습관**: 매일 5분 vs **주 1~2회 몰아서**(장기 유지자에게 더 많다) · 상위 항목 5~10개만 · 부부 카드는 명의별 · 무지출일 형광펜 · **월말 잔액 대조 → 누락 보충 → 다음 달 예산 5% 조정** · 3개월 쌓여야 패턴 · 스프레드시트 열: 날짜·항목·카테고리·금액·결제수단·카드·할부개월·비고 + Plan(항목×월) + Monthly/Annual Report. "매트릭스형은 거래 정보(날짜·가맹점·수단)가 사라지고, 목록형은 연간 요약이 약하다 → 둘을 결합한 템플릿(Note → Plan → Report)이 팔린다."

출처: [편한가계부 App Store](https://apps.apple.com/kr/app/%ED%8E%B8%ED%95%9C%EA%B0%80%EA%B3%84%EB%B6%80/id560481810) · [뱅크샐러드 엑셀](https://www.banksalad.com/articles/%EA%B0%80%EA%B3%84%EB%B6%80-%EC%97%91%EC%85%80-%EC%96%B4%ED%94%8C-%EC%B6%94%EC%B2%9C-%EC%96%91%EC%8B%9D) · [토스피드](https://toss.im/tossfeed/article/toss-user-interview-timeline) · [위플 리뷰](https://apps.apple.com/kr/app/%EC%9C%84%ED%94%8C-%EA%B0%80%EA%B3%84%EB%B6%80-weple-money/id467936485?see-all=reviews) · [똑똑가계부](https://appbiabi.com/clev-accountbook/) · [클머니 대체 스레드(클리앙)](https://www.clien.net/service/board/cm_iphonien/18947951) · [브런치 — 토스/뱅샐 지출 정의](https://brunch.co.kr/@jwj8906/23) · [노션 가계부(카드 이중 기록)](https://www.postype.com/@notion-flex/post/20706978) · [더쿠 — 편한가계부 마감 습관](https://theqoo.net/square/1625971770) · [크몽 구글시트 템플릿](https://kmong.com/gig/362019) · [예스폼 2026 가계부](https://blog.yesform.com/entry/2026%EA%B0%80%EA%B3%84%EB%B6%80%EC%97%91%EC%85%80) · [블라인드 부부 가계부](https://www.teamblind.com/kr/post/%EB%B6%80%EB%B6%80-%EA%B0%80%EA%B3%84%EB%B6%80-%EA%B3%B5%EC%9C%A0%EC%96%B4%ED%94%8C-%EB%AD%90-%EC%8D%A8-z2Rearbf)

### 3.3 방법론·행동 근거

- **YNAB 4원칙**: ① 모든 돈에 용도 배정 ② 불규칙 지출을 월 적립(true expenses) ③ 초과하면 다른 카테고리에서 옮긴다("예산은 돌에 새긴 게 아니다") ④ Age of Money. Actual은 "이미 가진 돈만 배정"·"남은 돈은 이월"·"초과는 다음 달 To Budget에서 차감(기본) 또는 음수 유지" ([Actual](https://actualbudget.org/docs/budgeting/), [YNAB 4 rules](https://gofrombroke.com/ynab-four-rules)).
- **이월 정책 비교**: Actual 기본(양수 이월·초과 차감) / 'Rollover overspending'(음수 유지) / Quicken 3옵션(없음·전부·**양수만**) / YNAB 옮겨 덮기. "예산-실적 격차를 보는 순간의 부정적 감정이 회피로 이어진다"([econbrew](https://www.econbrew.com/post/why-budgeting-apps-fail-the-hidden-behavioral-aspects)) → 초과가 다음 달을 벌하지 않는 **양수만 이월**이 완충형.
- **싱킹펀드**: "남은 금액 ÷ 남은 개월"을 매달 재계산(Actual `#template 1200 by 2026-12`). ✅ 연단위 ÷12가 이미 그 형태.
- **월 마감 리추얼**: YNAB "대조(잔액 조정 거래를 만들고 넘어가라) → 잔액 리셋 → 재배정", Actual 'End of Month Cleanup'. **시스템 개입은 조용한 덮어쓰기가 아니라 명시적 조정 항목**으로 남긴다([YNAB](https://www.ynab.com/blog/get-back-on-track), [Actual cleanup](https://actualbudget.org/docs/experimental/monthly-cleanup/)).
- **환급·이체**: 환불은 원래 카테고리로 되돌린다. 리뷰 최다 불만 3종 = 이체 오인·환급 오인·중복([Actual returns](https://actualbudget.org/docs/budgeting/returns-and-reimbursements/), [MoneyPatrol](https://moneypatrol.com/moneytalk/budgeting/best-financial-budgeting-app-how-to-evaluate-accuracy/)).
- **대출**: YNAB Loan Planner(추가 납입 → 절약 이자·단축 개월·번다운 → 예산 카테고리에 반영)가 기대치 표준. 눈덩이 vs 눈사태는 "총이자·완료일을 나란히 보여 주고 선택은 사용자에게"(JMU 논문). 국내는 DSR·스트레스 DSR 계산기([YNAB Loan Planner](https://www.ynab.com/blog/ynab-loan-planner), [JMU](https://commons.lib.jmu.edu/honors201019/699/), [토스 DSR](https://toss.im/tossfeed/calculator/dsr)).
- **습관**: 생활 앱 100일 내 중단 중앙값 70%([PMC](https://www.ncbi.nlm.nih.gov/pmc/articles/PMC11694054/)). 국내 처방 "카테고리 5개 이하·입력 10초·빼먹은 날은 둔다·주 1회 5분 통계·3개월 뒤 현실적 예산"([인사이트24](https://insight24.co.kr/budget-app-habit-keep-going/)). 구체적 목표를 언급한 리마인더가 효과적(Karlan 2016, [INFORMS](https://pubsonline.informs.org/doi/10.1287/mnsc.2015.2296)). 게이미피케이션은 유능감→몰입 경로로만 작동하고 **끊긴 스트릭은 이탈을 촉발**(Bitrián 2021, [Decision Lab](https://thedecisionlab.com/insights/consumer-insights/streak-creep-the-perils-of-too-much-gamification)) → 스트릭·배지 대신 **이달 목표 대비 진행 + 월 결산**을 시각화.
- **데이터 모델**: 거래가 1차, 계층 카테고리, 계좌 in/off budget, '미입력·미승인·근사'는 값이 아니라 **상태 플래그**(YNAB `approved/cleared`, Actual `~`), 삭제는 tombstone(YNAB `deleted`, Actual CRDT tombstone), 감사 로그는 실제 변경만(Firefly 교훈) ([YNAB OpenAPI](https://api.ynab.com/papi/open_api_spec.yaml), [Actual CRDT](https://archive.jlongster.com/using-crdts-in-the-wild), [Firefly #7963](https://github.com/orgs/firefly-iii/discussions/7963)).

### 3.4 이 앱에 대한 결론

이미 가진 것(월 예산표·계획 자동 반영·연단위 적립·대출 계산·엑셀·null 계약)은 **조사 기준으로도 상위 수준**이다. 빠진 것은 정확히
**거래 레이어와 그 위에 서는 기능(달력 일별·진행률·확인·카드 주기·이체·분할·검색)**, 그리고 **상환표·순자산·월 마감**이다.
자동연동·사진·다중통화·공동편집은 조사에서도 마찰·불신·종료 리스크의 원천으로 나타나므로 이 앱의 "로컬 우선·수기·엑셀 백업" 노선을 유지한다.

---

## 4. 재설계 원칙 (10)

1. **두 레이어**: 계획 레이어(월 매트릭스)와 기록 레이어(거래 원장)를 분리하되, 실제 금액은 **거래가 있으면 거래 합**이 단일 소스다.
2. **기존 습관 무손실**: 거래를 한 건도 넣지 않는 사용자는 지금과 **1원도, 1픽셀도** 다르지 않게 동작한다(하위호환의 축 — 검증으로 고정).
3. **미입력 ≠ 0원**을 거래 레이어에도 확장한다(거래 0건 ≠ 지출 0원 — §5.3.3). 근사·미확인·자동생성은 값이 아니라 **상태**(`origin`)로 표현.
4. **파생값 저장 금지**: 잔액·청구액·상환표·진행률·순자산·이월은 전부 매 렌더 파생. 저장은 사용자 입력만.
5. **영속화 신규 지점 0곳**: 거래·계좌·목표·마감을 전부 `LedgerBook` **안**에 둔다(BtConfig 선례). 스냅샷·sticky·지문·정규화·브릿지가 자동 상속.
6. **입력 마찰 최소화**가 습관 형성의 전부다: 빠른 입력 바(금액 → 항목 자동완성 → Enter), 오늘 기본값, 최근 항목, 키보드 완결, 미분류 허용.
7. **조용한 오적용보다 명시적 미적용**: 계산 불가·비교 불가·미분류·수동값 충돌은 0이 아니라 `-`/배지로 드러낸다. 시스템 개입(대조 조정·자동 기입)은 **명시적 거래**로 남긴다.
8. **한 화면 한 규칙**: 같은 이름의 숫자가 두 값이 되지 않게 한다(계획 기준/실적 기준을 라벨로 분리 — 기존 규약).
9. **색·팔레트 불변**: 새 화면도 `LEDGER_GROUP_COLOR`·`LEDGER_DIVERGING`·`ledgerRamp`만 쓴다(새 hue 금지 — `validate_palette` §4가 색 공간 포화를 단언).
10. **검증 우선**: 새 순수 함수는 직접 import 테스트 + 변이 테스트, 배선은 사용부 가드. 픽스처의 실측치(544,059 / 62,743,196 / 27.6%)는 고치지 않는다.

---

## 5. 목표 설계

### 5.1 개념 모델

```
                ┌────────────── 계획 레이어 (기존) ──────────────┐
                │ 항목 × 월 매트릭스: plan / planOverride / actual │
                │ 대출·연단위·수입 · 예상(expected) · KPI          │
                └───────────────▲───────────────────────────────┘
                                │ 실제 = 거래 합(있으면) ?? 수동 actual
                ┌───────────────┴───────────────────────────────┐
                │ 기록 레이어 (신규): transactions[]               │
                │ 날짜·금액·항목(분할)·결제수단·계좌·메모·할부·환급·누가 │
                └───────────────▲───────────────────────────────┘
                                │ 계좌(현금/통장/카드) · 카드 청구주기 · 이체 · 잔액 대조
                ┌───────────────┴───────────────────────────────┐
                │ 자산 연결 (신규, 읽기 전용): 순자산 =            │
                │  가계부 계좌 잔액 + 앱 포트폴리오 평가액 − 대출 잔액 − 카드 미결제 − 마통 │
                └───────────────────────────────────────────────┘
```

### 5.2 데이터 모델 변경 (`src/ledger.ts`)

모든 신규 필드는 **선택**이며 정규화가 기본값을 채운다. 저장 위치는 전부 `LedgerBook` 안.

```ts
// ── 신규: 거래 ──
export type LedgerTxKind = 'expense' | 'income' | 'transfer';
export type LedgerTxOrigin = 'manual' | 'confirm' | 'auto' | 'import' | 'adjust' | 'migrate';
export interface LedgerTxSplit { itemId: string; amount: number; memo: string; }
export interface LedgerTx {
  id: string;
  date: string;              // 'YYYY-MM-DD' (KST 달력일 — 창에서는 브릿지의 today를 기본값으로)
  amount: number;            // 항상 양수. 환급/취소는 refund:true (부호 대신 플래그 — 집계에서 부호 실수 방지)
  refund: boolean;
  kind: LedgerTxKind;        // expense/income은 항목 group에서 파생되지만 **저장**한다(항목 삭제 뒤에도 집계 가능)
  itemId: string;            // '' = 미분류(빠른 입력 허용) → '변동비·미분류' 가상 행으로 집계. splits가 있으면 무시
  splits: LedgerTxSplit[];   // 분할. 비어 있으면 단일 항목. 있으면 Σsplits.amount === amount (정규화가 강제)
  pay: LedgerPay;            // 항목 기본값 승계 → 건별 변경 가능. accountId가 있으면 그 계좌의 pay와 같아야 한다(정규화)
  accountId: string;         // '' = 계좌 미지정(계좌 도입 전 데이터와 호환)
  toAccountId: string;       // kind==='transfer'일 때만(카드대금 결제 = 통장→카드)
  memo: string;              // ≤ 120자
  payer: string;             // '' = 미지정. 누가 썼는가(부부 카드 명의 구분 — 자유문자열, 장부 레벨 payers 제안 목록)
  installmentMonths: number | null;  // 할부 개월(≥2). null = 일시불. 월 분배는 파생(§5.3.6)
  origin: LedgerTxOrigin;    // manual=직접 / confirm=고정지출 ✔ / auto=자동 기입 / import=가져오기 / adjust=잔액 대조 / migrate=수동값 이전
  originKey: string;         // confirm·auto의 멱등 키 `${itemId}|${ym}` (같은 키가 있으면 다시 만들지 않는다)
  deletedAt: string;         // '' = 살아 있음. 'YYYY-MM-DD' = 휴지통(§5.3.9)
  createdAt: number;
}

// ── 신규: 계좌(결제수단의 구체화) ──
export type LedgerAccountType = 'cash' | 'bank' | 'card' | 'other';
export interface LedgerAccount {
  id: string; name: string; type: LedgerAccountType;
  pay: LedgerPay;            // 이 계좌로 결제하면 어느 결제수단 축인가(소계·도넛 호환)
  closingDay: number | null; // card: 마감일(1~31). null이면 '사용월 = 청구월'
  billingDay: number | null; // card: 결제일(1~31). 휴일이면 다음 영업일(휴장일은 브릿지로)
  creditLimit: number | null;// card: 한도(잔여 한도 표시용, 선택)
  openingBalance: number | null;  // cash/bank: 잔액 추적 옵트인(둘 다 있어야 표시)
  openingAsOf: string;       // 'YYYY-MM-DD'
  linkedPortfolioId: string; // 앱 계좌(마통·직접입력)와 연결(표시 전용, 선택)
  archived: boolean; createdAt: number;
}

// ── 신규: 저축 목표 ──
export interface LedgerGoal {
  id: string; name: string; target: number; deadline: string;   // 'YYYY-MM'
  itemId: string;            // 적립을 담는 항목(예: '적금'). Σ actualResolved가 진행률
  createdAt: number;
}

// ── 기존 타입 확장 ──
export interface LedgerItem {
  …기존…
  entry: 'monthly' | 'tx';   // 실적 입력 방식. 기본 'monthly'(레거시 무변경). 첫 거래를 넣는 순간 'tx'로 자동 전환(§5.3.2)
  dueDay: number | null;     // ⚠️ annual 전용에서 **전 그룹**으로 확장(고정비·대출 납부일). annual은 dueMonth+dueDay 그대로
  autoPost: boolean;         // true면 dueDay가 지난 달에 계획 금액 거래를 자동 기입(origin 'auto', 멱등). 기본 false(= ✔ 확인 방식)
  carryover: 'off' | 'positive' | 'all';  // 이월. 기본 'off'. 'positive' = 남은 계획만 이월(YNAB/Quicken), 'all' = 초과도 차감
  defaultAccountId: string;  // 빠른 입력 기본 계좌
}
export interface LedgerMonthMeta {
  touchedDate: string; memo: string;
  closed: boolean; closedAt: string;    // 월 마감(§5.3.8). 기본 false/''
}
export interface LedgerLoan {
  …기존…
  extraPayments: Record<string, number>;   // {[ym]: 추가 상환액} — 실제로 낸 조기상환(사실이므로 저장)
  rateChanges: Record<string, number>;     // {[ym]: 연 %} — 그 달부터 적용되는 금리(변동금리)
  prepayMode: 'shortenTerm' | 'reducePayment';  // 추가 상환 후 재계산 방식(기본 shortenTerm)
}
export interface LedgerBook {
  …기존…
  transactions: LedgerTx[]; accounts: LedgerAccount[]; goals: LedgerGoal[];
  payers: string[];          // '누가' 제안 목록(자유 입력값의 합집합은 파생 — categories와 같은 규약)
  rules: { id: string; keyword: string; itemId: string; createdAt: number }[];  // 가져오기·자동완성 매칭 규칙(단계 E)
}
```

**상한(신규 상수)**: `MAX_LEDGER_TX = 6000`(장부당 ≈700KB) · `MAX_LEDGER_TX_MEMO_LEN = 120` · `MAX_LEDGER_TX_SPLITS = 8` · `MAX_LEDGER_ACCOUNTS = 20` ·
`MAX_LEDGER_GOALS = 20` · `MAX_LEDGER_INSTALLMENT_MONTHS = 60` · `MAX_LEDGER_RULES = 200` · `MAX_LEDGER_PAYERS = 10`. 80% 도달 시 헤더 배너(연도 정리 안내 §5.6).

**정규화(`normalizeLedgerBooks`) 규칙 추가**
- `transactions`: 배열 아니면 `[]`; 각 건 `date` 유효(`isValidLedgerDate`)·`amount` 유한·`kind`/`pay`/`origin` 화이트리스트·id 문자열·`installmentMonths` 2~60 정수 또는 null·`deletedAt` 유효 날짜 또는 ''·`splits` 각 항목 유한 금액, **Σsplits ≠ amount면 splits를 버리고 단일 항목으로 강등**(조용한 합계 오류 방지). 상한 초과분은 **오래된 것부터** 버린다(정렬: date desc, createdAt desc).
- `accounts`·`goals`·`rules`·`payers`: 화이트리스트 + 상한. `items[].entry` ∈ {'monthly','tx'} 기본 'monthly'; `autoPost` boolean; `carryover` ∈ {'off','positive','all'} 기본 'off'; `defaultAccountId` 문자열.
- `months[].closed` boolean, `closedAt` 유효 날짜 또는 ''. `loan.extraPayments`/`rateChanges`: `normMoneyMap`(ym 키만). `prepayMode` 화이트리스트.
- ⚠️ **멱등 계약 유지**: 필드가 없는 레거시 장부는 기본값으로 채우되, `same` 판정에서 `undefined`와 기본값을 같게 본다(`sameStrList`의 선례 — 안 그러면 레거시 장부가 로드마다 '변경됨'이 되어 Drive 폴링마다 재저장 + 로컬 사본 갈아엎기).
- ⚠️ 정규화는 **휴지통을 비우지 않는다**(시간에 따라 결과가 달라지면 멱등이 깨진다). 비우기는 사용자 동작(§5.3.9).

**지문(`ledgerFingerprint`) 투영 추가**: `x:` 거래(전 필드, `createdAt` 포함 — 같은 값 재입력을 구분), `a:` 계좌, `g:` 목표, `r:` 규칙, `p:` payers,
`items[].entry/autoPost/carryover/defaultAccountId/dueDay`, `months[].closed/closedAt`, `loan.extraPayments/rateChanges/prepayMode`.
⚠️ 길이·개수 해시 금지(기존 규약). ⚠️ **참조 캐시**: 지문 계산은 App 저장 effect 첫 블록에서 매번 돈다 — 거래 6,000건이면 stringify가 매 렌더 수 ms →
`WeakMap<books배열, string>` 캐시(같은 참조면 재계산 없음. 순수성 불변 — 검증이 "같은 참조 두 번 = 같은 문자열, 새 참조 = 재계산"을 단언).

**`ledgerBooksHaveContent`**: 거래 1건 이상 / 계좌 이름 있음 / 목표·규칙 있음도 '내용 있음'.

**스냅샷(`ledgerSnapshots`)**: ⚠️ **거래는 스냅샷에 넣지 않는다**(`books`를 `transactions: []`로 벗겨 저장하고 `txStripped: true` 표시).
근거: 512KB 예산에 700KB 거래를 넣으면 `pushLedgerSnapshot`이 이전 스냅샷을 전부 버려 사실상 1개만 남는다. 거래의 안전망은 휴지통(§5.3.9)+세션 undo이고,
스냅샷은 **계획 매트릭스·수동 실적·설정**의 안전망이다. 복원 시 현재 거래는 **그대로 유지**(복원이 거래를 지우지 않는다) — 화면 문구로 명시.
`ledgerSnapshotSummary`에 `txStripped` 표시 추가.

### 5.3 파생 규약

#### 5.3.1 거래 인덱스 — `txIndexOf(book)`
```ts
interface LedgerTxIndex {
  byItemYm: Map<string /* itemId|ym */, { sum: number; count: number }>;  // 분할·할부 분배·환급 반영, 휴지통·transfer 제외
  byYm:     Map<string, { expense: number; income: number; count: number }>;
  byDate:   Map<string, { expense: number; income: number; count: number }>;
  byAccountYm: Map<string, { out: number; in: number; count: number }>;   // 잔액·카드 청구용(transfer 포함)
  uncategorizedYm: Map<string, { sum: number; count: number }>;          // itemId==='' && splits 없음
  byPayerYm: Map<string, { expense: number; count: number }>;
  firstTxYm: Map<string /* itemId */, string>;                            // 항목별 첫 거래 월(entry 자동 전환·미입력 판정)
}
export const txIndexOf = (book) => …  // ⚠️ WeakMap<LedgerBook, LedgerTxIndex> 캐시. 장부 객체가 불변(편집마다 새 객체)이라 참조 캐시가 곧 정확한 무효화다.
```
- 순수·결정적(같은 book → 같은 결과). 검증이 직접 import해 캐시 유무와 무관하게 값을 단언한다. 비용 O(거래 수) 1회.
- 분할 거래는 각 split의 `itemId`로 나뉘어 `byItemYm`에 들어가고, `byYm/byDate`에는 원 거래 1건으로 센다.

#### 5.3.2 실제 금액의 단일 소스 — `actualResolved(item, ym, ix)`
```
if ix.byItemYm.has(item.id|ym) → { value: sum, source: 'tx', count }      // 거래가 1건이라도 있으면 거래 합
else if actual[ym] 키 있음        → { value: actual[ym], source: 'manual' }
else                              → { value: null, source: 'none' }
```
- ⚠️ `actualOf(item, ym)`(기존, 수동 값만)는 **이름·의미를 유지**한다(레거시 테스트 불변). 화면·집계는 전부 `actualResolved`로 옮긴다.
  집계 함수는 **`ix`를 받는다**: `monthTotals(book, ym)`처럼 book을 받는 함수는 내부에서 `txIndexOf(book)`(시그니처 무변경);
  items 배열을 받는 `expectedTotal(items, ym, ix)`·`expectedIncomeTotal`·`expectedByPay`는 3번째 인자(없으면 종전 = 수동만, 하위호환).
  ⚠️ 소스 가드: `LedgerPage.tsx`·`ledgerExcel.ts`·`CalendarModal.tsx`의 **모든** 호출이 3번째 인자를 넘긴다(빠뜨리면 거래가 조용히 무시돼 합계가 줄어든다 — 이 저장소가 가장 싫어하는 실패 모드).
- **수동 값과 거래의 충돌**: (항목, 월)에 수동 `actual[ym]`이 있는데 첫 거래를 넣으면 화면이 **한 번 묻는다** —
  `수동 입력 ₩X가 있습니다 → [거래로 옮기기(권장)] [수동 값 삭제] [취소]`. '거래로 옮기기'는 `date = ym-01`, `origin:'migrate'`, `memo='수동 입력 이전'` 거래 1건을 만들고 `actual[ym]`을 지운다.
  결정 전에도 거래 추가를 막지 않고(입력 마찰 금지) 셀에 `수동 ₩X 무시됨` 배지를 띄운다(조용한 오적용 금지).
- **`entry` 자동 전환**: 항목에 첫 거래가 생기면 `entry:'tx'`. 설정에서 'monthly'로 되돌릴 수 있다(그때도 거래가 있으면 거래 합이 이긴다 — 단일 소스 원칙).

#### 5.3.3 미입력 판정의 확장 — `expectsActual`·`missing`
- `entry:'monthly'`(레거시): 지금 그대로.
- `entry:'tx'`: (a) 거래 ≥1건 → 입력됨. (b) 0건 & `ym < 이번 달` & 미마감 & `ym ≥ firstTxYm` → **미입력**(nudge). (c) 0건 & 이번 달 → **진행 중**(미입력 아님, 배지 '이달 진행 중'). (d) 0건 & 마감됨 → **0원 확정**. (e) `ym < firstTxYm` → 종전 수동 규칙(거래 도입 이전 달은 수동 값의 세계).
- `compareMonths`(전월·전년)는 종전대로 **닫힌 달끼리**만 확정한다. 진행 중인 달은 새 함수 `mtdCompare(book, ym, today, ix)`:
  `이달 1일~오늘` Σ거래 vs `전달 1일~같은 일수` Σ거래(거래 있는 항목만, 일수 정렬). 전달 일수가 부족하면(31일 vs 30일) 말일로 캡. 표시 '같은 기간 대비 ▲/▼ · N일 기준'. `comparable=false`면 `-`.

#### 5.3.4 예산 진행률 — `monthProgress(book, ym, today, ix)`
```
항목별: plan(effectivePlan) · spent(actualResolved) · ratio = spent/plan · remain = plan − spent
        · daysLeft = 말일 − today + 1 · dailyAllowance = remain / daysLeft (remain ≤ 0 이면 0)
그룹/전체(변동비 합계): pace = spent / (plan × 경과일/전체일) — 1.0 초과 = 예산 소진 속도 초과
```
- null 계약: plan 없음 → ratio null(`'-'`). 과거 달은 `today = 말일`로 고정(진행률 = 결과). 미래 달은 계산하지 않는다.
- 색: `ratio ≤ 0.8` teal(`LEDGER_DIVERGING.under`) / `0.8~1.0` flat / `> 1.0` amber(over) + ▲ 아이콘. 새 hue 금지.

#### 5.3.5 이월(carryover) — `effectivePlan(item, ym, ix)`
- `'off'`(기본): `planOf` 그대로.
- `'positive'`: `planOf(ym) + max(0, effectivePlan(prev) − actualResolved(prev))` — 남은 계획만 이월(초과는 다음 달을 벌하지 않는다: Quicken '양수만'·econbrew 근거).
- `'all'`: 초과분(음수)도 차감(Actual 기본과 같은 엄격형).
- 재귀는 **활성 시작월(또는 첫 거래·첫 수동 값 월)에서 끊는다**. `actualResolved`가 null인 달(미입력)은 이월 계산에서 계획을 전액 쓴 것으로 보지 않고 **그 달을 건너뛴다**(미입력을 0으로 단언하지 않는다).
- ⚠️ 저장하지 않는다. 화면은 `+이월 ₩X` 표기. 계산량 O(개월) — 항목당 ≤ 12회.

#### 5.3.6 할부 분배 — `installmentCharges(tx, accounts)`
- `installmentMonths = n`(≥2)이면 `amount/n`을 **n개월에 걸쳐** 분배한다. 첫 회차 월 = 카드 청구월(계좌에 `closingDay`가 있으면 §5.3.7 규칙, 없으면 거래월). 잔여 1원 단위 오차는 마지막 회차에 흡수(Σ = amount, 검증 항등식).
- `byItemYm`·`byYm`·`byDate`는 **분배된 회차**로 집계한다(사용자 정의: "이번 달 할부금"이 이달 지출 — 똑똑가계부·같이가계부 규약). 원 거래는 목록에서 `할부 3/12` 배지.
- ⚠️ 발생주의(구입 시 전액)로 바꾸지 말 것 — 월 예산표 모델과 어긋난다. 대안이 필요하면 분석 탭에 '구입 기준 보기' 토글을 **표시로만** 둔다(집계 이중화 금지). **§9 결정 1**.

#### 5.3.7 카드 청구주기 — `cardCycleOf(account, ym)` / `cardBillOf(book, accountId, ym, ix, holidays)`
- `closingDay = D`, `billingDay = P`: 청구월 `ym`의 사용 구간 = `(전월 D+1일) ~ (당월 D일)`(D가 그 달에 없으면 말일로 클램프). `D === null`이면 사용월 = 청구월.
- 청구액 = Σ(구간 내 카드 거래 일시불) + Σ(그 청구월에 떨어지는 할부 회차) − Σ(구간 내 환급). 잔여 한도 = `creditLimit − 미결제 총액`(한도가 있을 때만).
- 결제일 = `ym의 P일` → 휴일이면 **다음 영업일**(`nextBusinessDay(date, holidaySet)` — 순수 함수, `backtest.ts`의 `shiftBusinessDays` 선례. 휴장일 집합은 `ledger:live`에 `holidays`로 동봉. 없으면 주말만 판정: graceful degradation). 클머니의 "휴일 시 익일 처리"가 이 기능.
- 카드대금 결제는 **이체 거래**(`kind:'transfer'`, 통장→카드)로 기록한다 → 지출 합계에 두 번 들어가지 않는다(토스·카카오페이·노션 이탈 사례의 이중 계상을 구조로 막는다). 빠른 입력에 '카드대금 결제' 프리셋(금액 기본값 = 그 달 청구액).

#### 5.3.8 월 마감 — `closeMonth(book, ym, today)` / `reopenMonth` / 잔액 대조 `reconcileAccount`
- 마감 시: `months[ym].closed = true, closedAt = today`, 그 달 `entry:'tx'` 항목의 0건은 0원 확정(§5.3.3 d), 자동 스냅샷 1개(`label: 'YYYY-MM 마감'`, `auto:true`).
- 마감된 달의 셀·거래는 **읽기 전용**(해제 버튼으로 재개방, 2단계 확인). 마감 요약 카드: 계획 대비·전월 대비·상위 5 항목·미분류 잔여·**다음 달 계획 제안**(최근 3개월 평균 — Actual `average 3 months` 템플릿; 버튼을 눌러야 `plan`을 쓴다. 과거 달은 `planOverride` 규약으로 불변).
- **잔액 대조**(계좌에 `openingBalance`가 있을 때): 실제 잔액 입력 → 차이 → `origin:'adjust'` 조정 거래 생성(YNAB 잔액 조정 거래 패턴. 조용한 덮어쓰기 금지). 마감 카드의 첫 단계로 배치(장기 사용자 습관 "월말 잔액 대조 → 누락 보충").
- ⚠️ 마감은 데이터를 바꾸지 않는다(플래그·조정 거래뿐) — 지문에 `closed/closedAt`이 들어가므로 저장은 트리거된다.

#### 5.3.9 휴지통·undo
- 거래 삭제 = `deletedAt = today`(소프트). 휴지통 화면에서 복원/영구 삭제, "30일 지난 항목 비우기" 버튼(사용자 동작 — 정규화가 자동으로 비우지 않는다).
- 세션 undo: 마지막 삭제/일괄 삭제 1회를 인라인 `되돌리기`(z-1090 위라 앱 토스트가 안 뜨므로 **화면 내 인라인**, 8초).
- 집계는 `deletedAt !== ''`를 전부 제외한다(인덱스 생성 시 한 번).

#### 5.3.10 대출 상환표 — `buildLoanSchedule(loan, {fromYm, months?})`
- 회차별 `{ym, payment, interest, principal, balanceAfter, rate, source}`. **원리금균등**은 `interest_k = balance_{k-1} × i_k`, `principal_k = payment − interest_k`(기존 `interestPart = P×i` 고정을 **회차별로 정정**).
  ⚠️ `loanSchedule(loan, ym).payment`는 기존 픽스처(544,059·353,875·423,289)와 **1원도 달라지면 안 된다** — 바뀌는 것은 `interestPart/principalPart` 분해뿐(검증 #9~#12가 payment만 단언하는지 확인하고, 분해 항등식 테스트를 새로 둔다: Σprincipal = P, 마지막 balance ≈ 0 ±1).
- `extraPayments[ym]`: 그 달 잔액에서 차감 → `prepayMode:'shortenTerm'`이면 납입액 유지·만기 단축, `'reducePayment'`면 잔여 기간으로 재계산.
- `rateChanges[ym]`: 그 달부터 `i` 교체 → 원리금균등은 잔액·잔여기간으로 재계산(변동금리 리셋).
- `paymentOverride`가 있으면 상환표도 그 납입액을 쓰고 이자·원금만 계산(사용자 입력 권위 규약 유지).
- 파생 KPI: 총 잔여이자·완납 예정월·조기상환 what-if(패널 입력, 저장 안 함: 절감 이자·단축 개월 — YNAB Loan Planner 형식). 대출 2건 이상이면 **여유 자금 X를 어느 대출에 먼저 넣을지** 눈덩이(잔액 작은 순)/눈사태(금리 높은 순) 총이자·완료일 병기(선택은 사용자).

#### 5.3.11 순자산 — `netWorth(book, ym, ix, assets)`
- `assets`는 앱이 `ledger:live`에 동봉(`{ totalEval, matongDebt, byAccount:[{id,name,accountType,eval}] }`, 원값 — 창에서 `hideAmounts` 마스킹). 인앱은 `intTotals`·`portfolioSummaries`에서 직접.
- 순자산 = Σ(가계부 cash/bank 잔액, `openingBalance` 있는 계좌만) + `assets.totalEval` − Σ(대출 `balanceAfter` 그 달) − Σ(카드 미결제) − `assets.matongDebt`.
  ⚠️ `linkedPortfolioId`로 연결된 가계부 계좌는 **중복 계상 방지**로 잔액 대신 앱 값을 쓴다. 자산 값이 안 왔으면(구버전·끊김) 카드가 `-`와 사유를 낸다(0 단언 금지). **§9 결정 5**(마통 부채 포함 여부).

### 5.4 화면 설계 (`LedgerPage.tsx` + 신규 컴포넌트)

⚠️ `LedgerPage.tsx`는 이미 2,153줄이다. 신규 탭은 **별도 컴포넌트**로 분리한다: `LedgerQuickEntry.tsx`(빠른 입력 바) · `LedgerTxTab.tsx`(거래) ·
`LedgerBudgetTab.tsx`(예산) · `LedgerAccountsTab.tsx`(계좌) · `LedgerLoanTab.tsx`(대출 — 기존 탭 이동) · `LedgerSettings.tsx`. `LedgerPage`는 셸(로컬 사본·승격·탭·헤더·매트릭스)로 남긴다.
전부 `// @ts-nocheck`, `../ledger` import는 **300자 이하 덩어리**로 나눈다(`undefcheck` 정규식 한계 — LedgerPage 헤더 주석 참조), 확인창은 인라인 2단계, `notify()` 금지, 상수·헬퍼는 모듈 스코프(`scopecheck` — `initTradeRest` 장애 선례).

#### 5.4.1 탭 구성 (4 → 7)
`거래` · `월 매트릭스` · `예산` · `계좌` · `대출` · `분석` · `연간` + 헤더 우측 `⚙ 설정`(구분·항목 설정·목표·규칙·휴지통·연도 정리·가져오기·마감 관리).
기본 탭 = 장부에 거래가 1건이라도 있으면 `거래`, 없으면 `월 매트릭스`(세션 로컬 기억). **빠른 입력 바**는 탭과 무관하게 하단 고정(접기 가능). **§9 결정 4**.

#### 5.4.2 빠른 입력 바 (`LedgerQuickEntry`, 모든 탭 하단, `readOnly`면 숨김)
```
┌────────────────────────────────────────────────────────────────────────────────────────┐
│ [◀ 2026-09-05 ▶] [금액 ________] [항목 🔍 ______▼] [결제/계좌 ▼] [메모 ______] [할부▼] [환급□] [누가▼]  ⏎ 추가 │
│ 최근: [식비·점심] [교통] [커피] [편의점] [주유] [마트]      모드: ●지출 ○수입 ○이체        ⋯ 분할  │
└────────────────────────────────────────────────────────────────────────────────────────┘
```
- 포커스 흐름: 금액 → 항목 → Enter. 항목 자동완성 = 이름 부분일치 + 최근 60일 빈도순(`suggestItems`) + 규칙 매칭(단계 E), 마지막 행 `새 항목 "병원" 만들기(변동비)`.
- 추가 후 금액만 비우고 나머지 유지(연속 입력). `Shift+Enter` = 추가 후 전부 초기화. 날짜 `←/→` 하루 이동, `T` 오늘. 결제수단/계좌는 항목의 `defaultAccountId` → `pay` 승계.
- 항목 미선택 Enter → **미분류 거래** 허용(배지). `⋯ 분할` = 행 확장(항목·금액 최대 8줄, 합계가 금액과 맞아야 추가 가능 — 불일치면 남은 금액을 마지막 줄에 채우는 버튼).
- 이체 모드: `계좌 → 계좌`(카드대금 결제 프리셋 = 이번 달 청구액), 지출 합계 제외.
- 폭 480px 미만: 2줄로 접힘(금액·항목 / 나머지). 매트릭스는 데스크톱 전용 그대로.
- ⚠️ 수동 값 충돌(§5.3.2)은 추가 직후 인라인 프롬프트로 처리(모달 금지 — z-1090·별도 창 규약).

#### 5.4.3 거래 탭 (`LedgerTxTab`)
```
[2026-09 ▼] [항목/구분 ▼] [결제 ▼] [계좌 ▼] [누가 ▼] [🔍 메모·항목 검색] [□환급만] [□미분류만 N]   합계: 지출 ₩1,234,000 · 수입 ₩3,000,000 · 42건
── 09-05 (금)  ₩23,400 ─────────────────────────────────────────────
  □ 식비/점심   김밥천국   카드·신한   4,500               [수정] [삭제]
  □ 교통        지하철     카드·신한   1,450  할부 —
── 09-04 (목)  ₩188,000 ────────────────────────────────────────────
  □ 쇼핑        …          카드·현대   180,000  할부 3/12 ⓘ
  □ 변동비·미분류  (항목 지정 필요)      8,000  ⚠
[선택 3건: 항목 일괄 변경 ▼] [삭제]   ┃ 오른쪽(≥1100px): 이달 예산 진행 미니 패널(변동비 상위 6 진행바) ┃ [주간 보기]
```
- 행 클릭 = 인라인 편집(날짜·금액·항목/분할·결제·계좌·메모·누가·할부·환급). 다중 선택 → 삭제(소프트)/항목 일괄 변경/누가 일괄 변경.
- 마감된 달은 편집 잠금 + 🔒 안내. 휴지통 링크(설정).

#### 5.4.4 월 매트릭스 (기존 + 변화)
- 거래가 있는 (항목, 월) 셀: 값 = 거래 합, **읽기 전용**, `N건` 배지, 클릭 = 거래 탭으로 이동(항목·월 필터 + 빠른 입력 항목 선점). 수동 셀은 지금 그대로. 분할 거래는 각 항목 셀에 자기 몫만.
- 항목 행에 `entry` 아이콘(⌨ 월 입력 / ≡ 거래 입력) — 설정에서 전환. 이번 달 열 헤더 `진행 중` 배지 + 소계 행 진행률 미니바.
- 고정지출 확인: 이번 달 셀이 '계획으로 채워진' 상태(`source:'none'` + plan)면 ✔ 버튼 → `origin:'confirm'`, `originKey = item|ym` 거래 1건(날짜 = dueDay ?? 오늘, 금액 = 계획). 같은 키가 있으면 no-op(멱등).
- 미분류 가상 행(변동비 그룹 끝): 그 달 미분류 합 + `정리` 링크. 소계·총계에 포함(실제 지출이므로).
- 마감된 달: 열 배경 강등 + 🔒, 셀 편집 불가.

#### 5.4.5 예산 탭 (`LedgerBudgetTab`, 신규)
```
이달(9월) 변동비 예산 ₩1,500,000 중 ₩980,000 사용 (65%) ▮▮▮▮▮▮▯▯▯▯  잔여 ₩520,000 · 25일 남음 · 하루 ₩20,800  · 속도 1.08 ▲(예산보다 빠름)
같은 기간 대비(전달 1~5일): ▼ ₩12,000
── 항목별 ──
식비        ₩600,000 ▮▮▮▮▮▮▮▮▯▯ 82%  잔여 ₩108,000 · 하루 ₩4,300   (+이월 ₩30,000)
교통        ₩150,000 ▮▮▮▯▯▯▯▯▯▯ 31%
…
── 고정지출 확인 (9월)  확인 3 / 미확인 4 ──
□ 월세 ₩700,000 (5일)   □ 통신 ₩65,000 (25일)  ☑ 보험 ₩120,000  ☑ 넷플릭스 ₩17,000   [전부 확인]
── 다가오는 지출 14일 ──
09-10 (화) 신한카드 결제 예정 ₩1,234,000 (마감 8/31 · 할부 2건 포함)   09-15 (일→16 월) 자동차세 ₩220,000   09-25 통신 ₩65,000
```
- '고정지출 확인'은 매트릭스 ✔와 **같은 `confirmItem` 헬퍼**(손복제 금지). `autoPost` 항목은 목록에 `자동` 배지로 뜨고, 페이지가 열릴 때(`dataState==='ready'` & 쓰기 가능) `postAutoItems(book, today)`가 dueDay가 지난 미기입 (항목, 월)에 `origin:'auto'` 거래를 만든다(멱등 키). 이번 달 미래 날짜는 만들지 않는다.
- '다가오는 지출'은 `upcomingDues(book, today, days, ix, holidays)` 단일 소스 — 달력 칩(§5.4.12)과 **같은 함수**.

#### 5.4.6 계좌 탭 (`LedgerAccountsTab`, 신규)
- 계좌 목록(유형·결제수단 축·잔액 옵션·연결된 앱 계좌). 카드: 마감일/결제일/한도 입력, **이번 달 청구 예정** 카드(구간·건수·할부 회차 포함), 다음 결제일(영업일 보정), 미결제 총액, 잔여 한도.
- 현금/통장: `openingBalance`를 넣은 계좌만 잔액 표시(= 시작잔액 + 수입 − 지출 ± 이체 ± 조정). 안 넣으면 `-`(잔액 추적은 옵트인). `대조` 버튼 → 실제 잔액 입력 → 조정 거래(§5.3.8).
- ⚠️ 잔액을 저장하지 않는다. 앱 포트폴리오(마통·직접입력)에 연결하면 그 계좌는 앱 값 표시(`linkedPortfolioId`).

#### 5.4.7 대출 탭 (`LedgerLoanTab`, 확장)
- 기존 표 유지 + 행 펼침 → **상환표**(회차·납입·이자·원금·잔액), `추가 상환`(ym·금액)·`금리 변경`(ym·%) 입력, `prepayMode` 토글.
- 하단: 총 잔여이자·완납 예정월·**조기상환 시뮬레이션**(금액·시점 what-if, 저장 안 함 — 절감 이자·단축 개월)·대출 2건 이상이면 눈덩이/눈사태 비교표.

#### 5.4.8 분석 탭 (확장)
- 기존 6카드 유지 + **구분별 12개월 추이(스택 막대, 구분 ≤5 + 기타)** + **구분별 전년 동월 표** + **상위 지출 TOP 10(거래 단위)** + **결제수단·계좌·누가별 월 합계**.
- 거래 기반 카드는 거래가 없으면 안내 문구(빈 차트 금지). 스택 색은 `ledgerRamp`(그룹 hue 유지).

#### 5.4.9 연간 탭 (확장)
- 연도별 전년 대비(기존) + **순자산 추이(월별)**(자산 값이 있을 때만) + 연단위 일정(기존) + 목표 진행(`LedgerGoal`).

#### 5.4.10 설정(⚙ `LedgerSettings`)
구분 관리(기존 `CategoryManager` 이동, `'/'` 구분자 = 2단 계층 `식비/외식` — 스키마 변경 없이 분석에서 트리 파생) · 항목 설정(entry·dueDay·autoPost·carryover·기본 계좌) ·
목표 관리 · 매칭 규칙(키워드→항목, 재분류에서 자동 생성 제안) · 누가(payers) · 휴지통 · **연도 정리**(§5.6) · **가져오기**(§5.4.11) · 마감/해제 · 거래 CSV 내보내기.

#### 5.4.11 가져오기(붙여넣기)
- 거래: 탭/쉼표 구분 텍스트 `날짜, 항목, 금액, [결제수단], [메모]` → 미리보기(항목명 매칭: 규칙 → 정확 → 부분 → '새 항목') → 반영. 실패 행은 사유와 함께 남기고 나머지만 반영(부분 성공 — Bluecoins 행별 오류 규약). `origin:'import'`. 중복 판정(같은 날짜·금액·항목) 경고.
- 매트릭스: `항목, 1월 … 12월` 12열 → 그 해 `actual[ym]` 수동 값(거래 없는 항목만; 있으면 건너뛰고 사유 표시).
- 파서는 순수 함수(`parseLedgerTxPaste`·`parseLedgerMatrixPaste`)로 직접 import 테스트. 한글 헤더·`₩`·쉼표·`-`/`▲▼` 부호를 허용.

#### 5.4.12 메모 달력 연동 (`CalendarModal`)
- BUDGET 칩 텍스트 우선순위: ① 그날 거래 합(`지출 ₩N · M건`) ② 카드 결제일/납부 예정 ③ 연단위 ④ 정리 기록. 한 칩에 요약, 패드에 그날 거래 목록(읽기 전용, 항목·금액·결제) + 예정 항목 + `가계부 열기`.
- `ledgerEventsByDate`에 `kind:'tx'`(집계)·`'due'`(고정비 납부일)·`'card'`(결제일) 추가 — `upcomingDues`와 같은 규칙. ⚠️ 거래를 `calendarMemos`에 복사 금지(라이브 파생 규약), `open` 게이트 유지.
- 달력 패드에서의 거래 추가는 **범위 밖**(App 쪽 writer 경로·`calendar:*` 신규 타입 필요 — 별도 단계 후보).

### 5.5 배선 변경표

| 대상 | 변경 | 근거 |
|---|---|---|
| App.tsx 영속화 7지점 | **변경 없음** — 거래·계좌·목표·규칙·마감이 `LedgerBook` 안 | 원칙 5 |
| `ledgerFingerprint` | 투영 확장 + WeakMap 참조 캐시 | §5.2 |
| `ledgerBooksHaveContent` | 거래·계좌·목표·규칙 포함 | §5.2 |
| 스냅샷 | `books`에서 `transactions` 제거 후 저장(`txStripped`), 복원 시 현재 거래 유지 | §5.2 |
| `ledger:live` payload | `+ holidays: string[]`(KR), `+ assets`(§5.3.11) — deps에 `marketHolidays`·`intTotals` 추가. ⚠️ `btHolidays` memo 선례처럼 **참조 안정** memo(매 렌더 새 배열 금지) | §5.3.7·§5.3.11 |
| `LedgerWindow` 수신 | 열거형 화이트리스트에 타입 추가 없음(기존 `ledger:live` 확장) — 새 타입을 만들면 **양쪽 등록** | 기존 |
| `calendar:live` | `ledgerBooks`에 거래 포함(용량↑) — 장부 참조가 바뀔 때만 전송됨을 확인 | 기존 |
| `ledgerExcel.ts` | 시트 ④ `거래내역`(날짜·항목·구분·결제·계좌·누가·금액·환급·할부·메모·출처) + ① 매트릭스 실제 열 = `actualResolved` + ② 대출 시트에 상환표 블록 | §5.4 |
| `CalendarModal` | 칩·패드 확장(§5.4.12) | — |
| Apps Script | **변경 없음**(게이팅 M열 그대로) | — |

### 5.6 용량·성능 정책

- 거래는 STATE 안(장부당 6,000건 ≈ 700KB). 근거: 영속화 신규 지점 0곳·sticky·브릿지 자동 상속·백업 22본에 자동 포함(별도 파일이면 백업 0본 — STOCK 선례).
  **분리 파일 이관 기준**(후속): STATE 2MB 초과 또는 장부 거래 6,000건 도달 시 별도 Drive 파일(`portfolio_ledger.json`, STOCK과 같은 cache-first·dirty 참조 비교)로 이관 — 후속 설계 항목. **§9 결정 3**.
- **연도 정리(archive)**: 설정에서 `YYYY년 거래를 월 합계로 접기` → 그 해 (항목, 월) 거래 합을 `actual[ym]` 수동 값으로 옮기고 거래를 삭제(엑셀 ④ 내보내기를 먼저 권장, 2단계 확인, 직전 자동 스냅샷). 항목별 월 합계는 무손실, 건별 상세만 사라진다.
- 렌더 비용: `txIndexOf` WeakMap 캐시 + 12개월 집계는 Map 조회. 거래 목록은 월 필터 기본이라 DOM ≤ 수백 행(가상화 불필요).
- 지문: 참조 캐시로 저장 effect 비용 상수화.

### 5.7 하위호환·마이그레이션

- 레거시 장부(거래 0건, `entry` 없음)는 정규화가 기본값을 채우되 **첫 로드 1회만 새 배열**(멱등) → 결과 값·화면 무변경. 검증: 기존 489건 그대로 통과 + "거래 0건이면 `actualResolved === actualOf`" 항등식 + 엑셀 ①~③ 셀 값 바이트 동일.
- `dueDay`가 annual 외 그룹에 생겨도 `expectsActual`·`planOf`는 annual만 dueMonth를 본다(의미 무변경).
- 별도 창 구버전 조합은 배포 단위가 같아 발생하지 않지만, `holidays`·`assets` 부재는 항상 허용(없으면 주말만·순자산 `-`).

---

## 6. 채택/보류 판정표 (조사 항목 → 이 설계)

| 조사 항목 | 판정 | 단계 | 이유 |
|---|---|---|---|
| 거래 원장(날짜·금액·항목·결제·메모) | **채택** | A | 모든 앱의 저장 단위. 현재 최대 공백 |
| 분할 거래 | 채택 | A | 오픈소스 공통 기본기, 뱅샐·머니매니저 결함으로 지적 |
| 누가 썼는지(payer) | 채택(경량) | A(모델)·F(분석) | 국내 부부 사용자 요구. 공동 편집 없이도 명의 구분은 가능 |
| 달력 일별 합계·패드 목록 | 채택 | A | 국내 앱 전부 달력 뷰 |
| 검색·필터·다중 선택 | 채택 | A | — |
| 소프트 삭제·undo | 채택 | A | YNAB/Actual tombstone, 되돌리기 없는 삭제는 이탈 원인 |
| 예산 진행률·하루 예산·속도 | 채택 | B | 뱅샐 일일 가능액이 유지 동기로 언급 |
| 고정지출 확인(✔) / 자동 기입 옵션 | 채택 | B | Actual '승인 vs 자동' 둘 다. 기본은 승인(안전) |
| 다가오는 지출 14일 | 채택 | B | HN "월말 놀람 줄이기" 요구 |
| 월 마감 + 마감 요약 + 다음 달 계획 제안 | 채택 | B | 장기 사용자 습관·YNAB/Actual 리추얼 |
| 이월(양수만/전부/끔) | 채택(옵트인, 기본 끔) | B | 정답이 하나가 아님. 위플 "이월 잔액 혼란" 불만 → 기본 끔 |
| 계좌·잔액(옵트인)·이체 | 채택 | C | 이중 계상 방지의 구조적 해법 |
| 카드 마감일·결제일·영업일 보정·할부·한도 | 채택 | C | 국내 최장수 요구(클머니·똑똑·같이가계부) |
| 잔액 대조 → 조정 거래 | 채택 | C | YNAB 대조 패턴·장기 사용자 월말 습관 |
| 대출 상환표·조기상환·금리변경·눈덩이/눈사태 비교 | 채택 | D | YNAB Loan Planner 표준·국내 엑셀 상환대장 |
| 순자산(앱 자산 연결) | 채택 | D | Mint 종료 이후 최다 요구, 앱이 이미 자산을 가짐 |
| 저축 목표 | 채택 | E | 뱅샐 "게임처럼" 동기. 경량 |
| 붙여넣기 가져오기 + 매칭 규칙 | 채택 | E | 이관 비용·규칙 자동 분류 요구(자동연동이 없으니 이 범위면 충분) |
| 연도 정리(압축) | 채택 | E | 용량 정책 |
| 구분 2단 계층(`/`) · 구분별 추이·전년 비교 · TOP10 · 주간 보기 | 채택 | F | Firefly "No sub categories" 불만·연간 요약 부재로 시트 이탈 |
| 모바일 폭 대응(거래 탭·빠른 입력만) | 채택 | F | 매트릭스는 데스크톱 전용 유지 |
| 급여일/카드 결제일 기준 월 경계 | **보류** | — | `ym` 키 전면 변경. 카드는 계좌 청구주기가 대신하고, 수기 고수들은 "1일 기준"을 권한다(더쿠) |
| 태그(직교 축) | 보류 | — | `memo` 검색 + `payer` + 구분 계층으로 대체. 요구가 실측되면 `tags: string[]` 추가(정규화·지문 2곳) |
| 무지출일 표시·스트릭·배지 | 보류/범위 밖 | — | 스트릭은 이탈 촉발 근거. 무지출일은 '미기록'과 구분 불가 |
| 미래 잔액 예측(N개월) | 보류 | F 후보 | 계좌 잔액 옵트인 + 예정 지출이 있어야 의미. 단계 C·B 뒤 재평가 |
| 문자/은행 자동 연동 · 영수증 사진 · 다중 통화 · 부부 공동 편집 · 위젯/푸시 · 달력에서 거래 입력 | **범위 밖** | — | 백엔드·보안·용량·writer 1 원칙·웹앱 한계 |

---

## 7. 단계별 구현 계획

각 단계 = 한 세션. 순서 고정(뒤 단계가 앞 단계의 함수를 쓴다). 각 단계 끝: `npm run gate` 전부 통과 → `git status --short`·`git diff` 확인 → 파일 명시 `git add` → 커밋(`feat(가계부): …`) → 푸시 → **배포 URL 확인 요청**.

### 단계 A — 거래 레이어 최소기능
**범위**: 모델·정규화·지문·haveContent·스냅샷 strip · `txIndexOf`·`actualResolved`·`mtdCompare`·`filterTx`·`suggestItems`·`addTx`/`updateTx`/`softDeleteTx`/`restoreTx`/`purgeTx` · 거래 탭 · 빠른 입력 바(분할 포함, 이체 모드는 단계 C) · 매트릭스 tx 셀 연동·미분류 행 · 수동값 충돌 프롬프트 · 달력 `kind:'tx'` · 엑셀 시트 ④ · 검증 · CLAUDE.md.
**파일**: `ledger.ts`, `LedgerPage.tsx`, 신규 `LedgerQuickEntry.tsx`·`LedgerTxTab.tsx`, `CalendarModal.tsx`, `ledgerExcel.ts`, `scripts/verify-ledger-tx.mjs`(신규, `package.json` `verify:ledger-tx` — gate가 `verify:*`를 자동 탐색), `scripts/verify-ledger.mjs`(스냅샷·지문·정규화 항목 갱신), `CLAUDE.md`.
**함수 시그니처(순수, ledger.ts)**
```ts
txIndexOf(book): LedgerTxIndex
actualResolved(item, ym, ix): { value: number|null; source: 'tx'|'manual'|'none'; count: number }
expectedTotal(items, ym, ix?) / expectedIncomeTotal(items, ym, ix?) / expectedByPay(items, ym, ix?)   // ix 없으면 종전
monthTotals(book, ym) / ledgerKpi(book, ym) / compareMonths(book, curYm, prevYm)                       // 내부 txIndexOf
mtdCompare(book, ym, todayDate): { cur, prev, days, delta, rate, comparable, reason }
filterTx(txs, { ym?, from?, to?, itemId?, category?, pay?, accountId?, payer?, q?, refundOnly?, uncategorizedOnly?, includeDeleted? }): LedgerTx[]
suggestItems(book, q, todayDate, ix): { itemId, name, group, score }[]
addTx(book, tx): { book: LedgerBook; conflict: { itemId, ym, manual: number } | null }   // 정렬 유지·상한·entry 자동 전환·충돌 보고
migrateManualToTx(book, itemId, ym) / dropManual(book, itemId, ym)
updateTx(book, id, patch) / softDeleteTx(book, ids, today) / restoreTx(book, ids) / purgeTx(book, ids)
stripTxForSnapshot(books): LedgerBooks   // 스냅샷 저장 전
```
**불변식(검증 #T1~)**: ① 거래 0건이면 모든 집계가 종전과 동일(기존 픽스처 그대로, 엑셀 ①~③ 바이트 동일) ② 휴지통 거래는 어떤 집계에도 없다 ③ 환급은 음수로 합산 ④ `byYm.expense === Σ byItemYm(지출 항목) + uncategorized` ⑤ 분할 Σ = amount, 정규화가 불일치를 강등 ⑥ `mtdCompare` 일수 정렬·말일 캡·comparable 규칙 ⑦ 수동값+거래 충돌 시 거래가 이기고 `conflict` 보고 ⑧ 미분류가 총계에 포함 ⑨ 정규화 멱등(레거시·신규) ⑩ 지문이 거래 필드 하나만 바꿔도 달라지고 같은 참조는 캐시 히트 ⑪ 스냅샷은 `transactions`를 벗기고 복원이 거래를 지우지 않는다 ⑫ `addTx`가 상한에서 거부하고 정렬을 유지.
**소스 가드**: 집계 호출부 3번째 인자 · Enter 경로가 `addTx` 경유 · tx 셀 `readOnly` · 삭제가 `softDeleteTx`(hard delete 금지) · 달력 `open` 게이트 · 엑셀 시트 수 4 · `calendarMemos`에 거래 복사 금지 · `../ledger` import 덩어리 ≤300자 · 신규 컴포넌트 `notify(` 0건.
**완료 기준**: 거래 30건 입력 → 매트릭스 셀이 합계로 잠기고 소계·KPI·달력 칩·엑셀 ④가 일치. 기존 489건 + 신규 ≥ 60건 통과, 변이 ≥ 10종 검출 확인(CLAUDE.md에 종류 기록).

### 단계 B — 예산 진행률 · 고정지출 확인/자동 기입 · 다가오는 지출 · 월 마감 · 이월
**파일**: `ledger.ts`(+`monthProgress`·`effectivePlan`·`upcomingDues`·`confirmItem`·`postAutoItems`·`closeMonth`/`reopenMonth`·`suggestPlanFromAverage`·`dueDay` 전 그룹), 신규 `LedgerBudgetTab.tsx`, `LedgerPage.tsx`(✔·마감 UI·진행 미니바), `CalendarModal.tsx`(kind 'due'), 검증, CLAUDE.md.
**불변식**: 진행률 null 계약 · pace 정의 · 이월 음수 없음(positive)·활성 시작월 끊김·미입력 달 건너뜀 · ✔/auto = 거래 1건, 멱등 키 · `postAutoItems`는 `today` 이후를 만들지 않는다 · 마감은 플래그+자동 스냅샷 1개 · 마감 달의 쓰기 헬퍼 전부 no-op(`patchItem`·`addTx` 경로 가드) · `upcomingDues`와 달력이 같은 함수.
**완료 기준**: 이달 변동비 진행바·하루 예산, 고정지출 체크 → 거래 생성, 14일 예정 목록 = 달력 칩, 마감/해제, 이월 옵션 동작.

### 단계 C — 계좌 · 카드 청구주기 · 할부 · 이체 · 잔액 대조
**파일**: `ledger.ts`(+`LedgerAccount`·`cardCycleOf`·`cardBillOf`·`accountBalance`·`installmentCharges`·`nextBusinessDay`·`reconcileAccount`), 신규 `LedgerAccountsTab.tsx`, `LedgerQuickEntry.tsx`(이체 모드·할부 select), `App.tsx`(`ledger:live`에 `holidays` — 참조 안정 memo), 검증, CLAUDE.md.
**불변식**: 할부 Σ회차 = 원금 · 청구구간 경계(D일 포함/제외)·말일 클램프 · 결제일 영업일 보정(휴장일 없으면 주말만) · 이체는 지출 합계 제외 · 잔액은 opening 있는 계좌만 · `pay` 축 소계 = 계좌별 합의 재분류와 일치 · 조정 거래는 `origin:'adjust'`이고 잔액 차이와 같다.
**완료 기준**: 카드 마감/결제일 설정 → '이번 달 청구 예정'이 거래·할부에서 산출, 카드대금 결제 이체가 지출에 안 잡힘, 대조가 조정 거래를 만든다.

### 단계 D — 대출 상환표 · 조기상환 · 금리변경 · 눈덩이/눈사태 · 순자산
**파일**: `ledger.ts`(+`buildLoanSchedule`·`loanRemaining`·`prepaySim`·`payoffCompare`·`netWorth`; `loanSchedule` 분해 정정), 신규 `LedgerLoanTab.tsx`(기존 탭 이동+확장), `LedgerPage.tsx`(연간 탭 순자산 카드), `App.tsx`(`ledger:live`에 `assets`), `ledgerExcel.ts`(② 상환표 블록), 검증, CLAUDE.md.
**불변식**: 기존 payment 픽스처 1원 불변 · Σ원금 = P · 마지막 잔액 ≈ 0 · extraPayments 뒤 잔액 감소·shortenTerm이면 payment 불변 · rateChanges 뒤 재계산 · override면 납입액 = override · 순자산은 자산 값 없으면 null · 연결 계좌 중복 계상 없음.
**완료 기준**: 상환표가 화면·엑셀에 뜨고 조기상환 what-if가 절감 이자·단축 개월을 낸다. 순자산 카드가 앱 자산과 합쳐진다.

### 단계 E — 목표 · 가져오기 · 매칭 규칙 · 연도 정리 · 휴지통 완성 · 거래 CSV
**파일**: `ledger.ts`(+`LedgerGoal`·`goalProgress`·`parseLedgerTxPaste`·`parseLedgerMatrixPaste`·`applyRules`·`suggestRuleFromRecategorize`·`archiveYear`·`buildTxCsv`), 신규 `LedgerSettings.tsx`(구분 관리 이동 포함), 검증, CLAUDE.md.
**불변식**: 파서 부분 성공·사유 행·중복 경고 · archive는 (항목,월) 합계 무손실·직전 자동 스냅샷·2단계 확인 · 목표 진행 = Σ actualResolved(연결 항목) · 규칙 적용은 자동완성 순위와 가져오기에만(저장된 거래를 소급 변경하지 않는다).

### 단계 F — 분석 확장 · 구분 계층 · 누가별 · 주간 보기 · 모바일 폭
**파일**: `LedgerPage.tsx`/분석 컴포넌트(구분 12개월 스택·구분별 전년 비교·TOP10·누가별·주간 보기·480px 대응), `ledger.ts`(`categoryTree`(`'/'` 파싱)·`topTx`·`byPayerSeries`), 검증(팔레트 재검증 — 새 hue 0), CLAUDE.md.

**CLAUDE.md 갱신 규칙(전 단계 공통)**: 가계부 절에 새 소절을 추가하되, 이 문서의 "왜"(사고·오독 경로)를 **⚠️ 되돌리면 재발** 형식으로 옮긴다. 검증 번호(#T·#G)와 변이 종수를 적는다. 이 설계 문서의 결정 번호(§5.3.x)를 인용해 두 문서가 어긋나지 않게 한다.

---

## 8. 검증 설계

- **새 스크립트 `scripts/verify-ledger-tx.mjs`**(`verify:ledger-tx`): 거래 레이어 이후 기능(단계 A~F) 전담. 기존 `verify-ledger.mjs`는 매트릭스·대출·엑셀 ①~③·팔레트 배선을 그대로 지킨다. 두 스크립트 모두 `src/ledger.ts`를 **직접 import**(미러 금지). gate는 `verify:*`를 자동 탐색하므로 `package.json` 등록만 하면 된다.
- 파트①(직접 import): 신규 함수마다 **동작 케이스 + 거래 0건 무영향 케이스**를 쌍으로(backtest 규약). 픽스처: 기존 스프레드시트 픽스처를 **덮어쓰기(over)로 확장**(원본 기대치 불변) + 거래 30건짜리 픽스처(분할·환급·할부·이체·휴지통·미분류 각 1건 이상 포함 — 경로를 실제로 밟아야 죽은 단언이 아니다).
- 파트②(소스 가드): 선언이 아니라 **사용부**를 단언. ① 집계 호출부 `ix` 전달 ② 삭제가 `softDeleteTx` 경유 ③ 스냅샷 `transactions` 제거 ④ 달력 `open` 게이트 ⑤ `ledger:live` `holidays`·`assets`가 참조 안정 memo ⑥ 마감 달 쓰기 no-op ⑦ ✔·자동 기입 멱등 키 ⑧ 이체가 지출 합계에 없음(음성 대조 포함) ⑨ 문구 가드는 구간을 잘라(`sliceBlock`) 단언, 부재 가드는 `stripComments` 뒤.
- 변이 테스트: 각 단계에서 ≥ 10종을 실제로 깨뜨려 검출을 확인하고 CLAUDE.md에 종류를 적는다. **하네스는 검증 스크립트가 정상 종료 요약을 냈을 때만 검출로 센다**(스크립트가 죽으면 전 변이가 '검출'로 위장 — memory 규약).
- `verify:excel`은 무수정(writer 계약), 시트 수·셀 값은 `verify:ledger`/`verify:ledger-tx`에서. `verify:palette`: 새 색 0.
- 성능 스모크(선택): 거래 6,000건 픽스처에서 `txIndexOf` 1회 < 20ms, `ledgerFingerprint` 캐시 히트 = 재계산 없음.

---

## 9. 구현 전 사용자 확인 항목 (결정 6건)

| # | 결정 | 제안(기본값) | 대안 |
|---|---|---|---|
| 1 | 할부 인식 방식 | **월 분배**(이번 달 할부금이 이달 지출 — 똑똑가계부·같이가계부) | 구입 시 전액(YNAB식) — 매트릭스 모델과 어긋남 |
| 2 | 스냅샷에서 거래 제외 | **제외**(거래는 휴지통+undo가 안전망) | 포함 — 스냅샷이 사실상 1개만 남는다 |
| 3 | 거래 상한 6,000/장부 + 연도 정리(엑셀 후 접기) | **동의** | 별도 Drive 파일 분리(백업 0본·부팅 순서 변경 필요) |
| 4 | 기본 탭 규칙 | 거래가 있으면 **거래 탭**, 없으면 매트릭스 | 항상 매트릭스 |
| 5 | 순자산에 마통을 부채로 포함 | 포함(`useIntegratedData.ts:43~54`의 `currentWithdrawal` 의미 확인 후) | 제외 |
| 6 | 이월 기본값 | **끔**(옵트인, 켜면 '양수만') | 켬 |

이 6건에 답이 없으면 구현 세션은 **제안(기본값)대로** 진행하되 커밋 메시지에 그 사실을 남긴다.

---

## 10. 범위 밖(의도) · 알려진 한계

**범위 밖**: 은행/카드 자동 연동·문자 파싱(백엔드·보안) · 영수증 사진(Drive 용량·뷰어) · 다중 통화(해외 자산은 포트폴리오가 담당) · 부부 공동 편집(writer 1 원칙 — `payer` 태그로 명의 구분만) · 위젯/푸시 알림(웹앱) · 달력 패드에서의 거래 입력(별도 단계 후보) · 급여일 기준 월 경계 · 태그 축.
**알려진 한계**: 인앱 폴백과 별도 창을 동시에 열면 마지막 쓰기가 이긴다(기존 절충). 거래가 많아지면 `calendar:live` payload가 커진다(장부 참조 변경 시에만 전송). 자동 기입(`autoPost`)은 페이지를 열어야 실행된다(서버 크론 없음 — Firefly의 03:00 cron과 다르다는 점을 설정 문구로 고지).

---

## 11. 다음 세션 시작 프롬프트 (복사용)

```
가계부 재설계 문서 `가계부_재설계_2026-09.md`의 §7 단계 A를 구현합니다.
먼저 문서의 §1(현재 구현)·§5(목표 설계)·§8(검증 설계)·§9(결정 6건)를 읽고, CLAUDE.md 가계부 절의 불변식을 지키세요.
§9의 결정은 [여기에 답을 적거나 "제안대로"라고 쓰세요].
범위는 단계 A만(거래 레이어·빠른 입력·매트릭스 연동·달력 칩·엑셀 ④·verify-ledger-tx). 다른 단계 기능을 미리 넣지 마세요.
끝나면 `npm run gate` 전부 통과 → 파일 명시 커밋·푸시 → 배포 화면 확인 요청까지.
```
단계 B 이후는 위 문장의 "단계 A"를 바꿔 쓰면 된다. 단계를 건너뛰지 말 것(뒤 단계가 앞 단계의 함수·검증을 전제한다).
